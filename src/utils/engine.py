"""
engine.py

Core evaluation engine that orchestrates the full pipeline:
parser → extractor → rules → score.
Will receive extracted data and apply rules to produce a green score.
Implementation will be added in a future step.
"""
